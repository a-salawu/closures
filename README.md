# closures
The idea of closures begin when a function keeps track of variables it uses outside its scope.

see the results of `example-1.js`
Check the scope of `console.dir(sum)` output. That is the simplest form of a closure